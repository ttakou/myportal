// The report letterhead is shared with the standalone offshore reports, so the
// implementation lives in @/components/ui. Re-exported here to keep the existing
// report-page imports stable.
export { ReportHeader } from "@/components/ui/report-letterhead";
